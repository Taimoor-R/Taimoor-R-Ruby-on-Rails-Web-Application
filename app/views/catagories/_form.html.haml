= form_for @catagory do |f|
  - if @catagory.errors.any?
    #error_explanation
      %h2= "#{pluralize(@catagory.errors.count, "error")} prohibited this catagory from being saved:"
      %ul
        - @catagory.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :Type
    = f.text_field :Type
  .actions
    = f.submit 'Save'
